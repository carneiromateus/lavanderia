<%= form_with(model: peca, local: true) do |form| %>
  <% if peca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peca.errors.count, "error") %> prohibited this peca from being saved:</h2>

      <ul>
      <% peca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :client_name %>
  </div>
    
    <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :client_value %>
  </div>   
   <div class="field">
    <%= form.label :active %>
    <%= form.text_field :active, id: :client_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
